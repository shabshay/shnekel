Build, test, commit, and deploy to production.

1. Run `npm test` in the app directory — all tests must pass
2. Run `npm run build` in the app directory — must succeed with zero errors
3. Stage all changes with `git add -A`
4. Show me a summary of what changed and draft a commit message
5. Commit and push to origin main
6. Watch the GitHub Actions deploy and confirm it succeeds
7. Report the live URL: https://shabshay.github.io/shnekel/

Show the current project status.

1. Run `git log --oneline -10` to show recent commits
2. Run `npm test` to check test status (pass/fail count)
3. Run `gh run list --repo shabshay/shnekel --limit 3` to show recent deploys
4. Check if there are any uncommitted changes with `git status`
5. Show the live URL: https://shabshay.github.io/shnekel/
6. Summarize: last deploy status, test count, any pending changes

Plan and implement a new feature for Shnekel.

Follow this workflow:
1. **Understand** — Ask clarifying questions if the feature is ambiguous
2. **Plan** — Enter plan mode, explore affected files, design the approach
3. **Implement** — Write the code following project conventions (see CLAUDE.md)
4. **Test** — Add unit tests for any new pure functions or business logic
5. **Verify** — Run `npm test` and `npm run build` — both must pass with zero errors
6. **QA** — Start the preview server, visually verify the feature works
7. **Deploy** — Use /deploy to ship it

Feature to implement: $ARGUMENTS
